# joystick-game-controller
Control games using an Arduino joystick and Python by converting joystick movements into keyboard arrow key inputs.


Control online/downloaded games with a physical Arduino joystick instead of a keyboard. 
The Arduino reads joystick movements and sends analog values to Python, which converts them into arrow key presses using PyAutoGUI.
